screenNoPlayLeft = --variable definissant la zone non jouable a gauche en pixels
screenNoPlayRight = --variable definissant la zone non jouable a droite en pixels
screenNoPlayTop = --variable definissant la zone non jouable en haut en pixels
screenNoPlayRight = --variable definissant la zone non jouable en bas en pixels